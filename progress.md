Original prompt: 每次最多的连击打破记录，我想记录到左侧。有一个列表，每次突破上一次最多连击，就记录到左侧列表里。连击次数和时间。

- 2026-03-13: 确认连击逻辑位于 `src/action.js`，左侧现有 UI 位于 `rush-panel`。
- 2026-03-13: 准备新增左侧“破纪录历史”列表，并使用 `localStorage` 持久化保存突破记录。
- 2026-03-13: 已新增左侧“破纪录历史”列表；只有当连续命中次数严格超过历史最高值时，才写入一条新记录，包含连击数与本地时间。
- 2026-03-13: 已通过 Playwright 验证 4 次连续破纪录后会生成 `x1` 到 `x4` 记录，并在页面刷新后继续保留；验证产物位于 `output/quick-hit-records-check.json` 与两张截图中。
- 2026-03-13: 按要求补充“清空记录”按钮，并加入二次确认；清空后会同步删除本地 `localStorage` 记录。
- 2026-03-13: 根据后续反馈，将记录列表改为独立放在最左列，并把历史记录上限从 50 条收紧为 10 条。
- 2026-03-13: 已验证左侧布局生效；连续打到 `x12` 后，界面与 `localStorage` 仅保留最近 10 条记录（`x12` 到 `x3`）。
- 2026-03-13: 再调整为默认保留 20 条记录，并为左侧列表增加最大高度与滚动，避免长列表把页面纵向撑开。
- 2026-03-13: 按最新反馈，将破纪录列表改为桌面端固定在页面最左侧；移动端继续走普通流式布局。
- 2026-03-13: 为当前连击文案减少换行，增大 `rush-panel` 宽度并收紧 `rush-streak` 的字号与字间距，优先保持单行显示。
- 2026-03-15: 根据最新反馈，把左侧历史改为“每段连击只记录一次”，不再在同一段连击中按 `x3 -> x4 -> x5` 逐次落库；改为断连时只写入该段峰值。
- 2026-03-15: 顺手修正断连后的面板刷新，避免内部连击已归零但界面仍停留在上一档位。
- 2026-03-15: 通过 Playwright 验证：预置历史 `x2` 后打出一段 `x5` 连击，断连前左侧仍只有 `x2`，断连后左侧变为 `x5`、`x2`；验证产物位于 `output/quick-hit-combo-end-record.json`、`output/quick-hit-combo-before-timeout.png`、`output/quick-hit-combo-after-timeout.png`。
